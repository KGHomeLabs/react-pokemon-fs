# Node modules
node_modules/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OSss
.DS_Store
Thumbs.db

# Build output
dist/
build/

Designs/
# Optional:
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# IDEs
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln

# Yarn offline mirror (optional; keep if using `--offline`)
.yarn/cache/
.yarn/unplugged/
.yarn/build-state.yml
.pnp.*
.yarn/install-state.gz
.yarn/patches/
# Yarn v1 lock file (if you're using Yarn 2+ with PnP, keep `yarn.lock`)
# yarn.lock  <-- KEEP THIS! (important for reproducible installs)